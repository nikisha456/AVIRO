// zoom time + hold time + fade time

setTimeout(()=>{
  document.body.classList.add("fadeOut");
},3500);   // 3.5 sec logo dikhega

setTimeout(()=>{
  window.location.href="login.html";
},4200);   // then next page
